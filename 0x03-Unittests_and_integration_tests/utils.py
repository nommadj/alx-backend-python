"""Module containing utility functions."""\n\nimport requests\n\ndef has_license(repo):\n    """Check if a GitHub repository has a license."""\n    if not isinstance(repo, dict):\n        return False\n    license_info = repo.get(\"license\")\n    return isinstance(license_info, dict) and license_info.get(\"key\") is not None\n\ndef get_repos(org):\n    \"\"\"Get public repositories for the given organization.\"\"\"\n    url = f\"https://api.github.com/orgs/{org}/repos\"\n    response = requests.get(url)\n    if response.status_code == 200:\n        return response.json()\n    return []\n\ndef get_license(repo):\n    \"\"\"Get license key of the repository or None.\"\"\"\n    if not isinstance(repo, dict):\n        return None\n    license_info = repo.get(\"license\")\n    if isinstance(license_info, dict):\n        return license_info.get(\"key\")\n    return None
